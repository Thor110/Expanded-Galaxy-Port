UI textures from Override are currently in "Z UI Sorting" folder

Cross reference with those in XboxGUI folder and sort the install_folders between all versions for the UI so that no duplicate files exist